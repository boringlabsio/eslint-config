# @hypedevs/eslint-config
Shared eslint config

Usage:
```bash
yarn add -D @hypedevs/eslint-config
```

```js
module.exports = {
  parserOptions: {
    project: './tsconfig.json',
  },
  extends: ['@hypedevs']
};
```